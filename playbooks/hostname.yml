---
- name: change hostname
  hosts: routers
  gather_facts: false

  tasks:
    - name: vaihda hostname
      cisco.ios.ios_hostname:
        config:
          hostname: "branch-{{ inventory_hostname | upper }}"
        state: merged
